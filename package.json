{
  "name": "sample-serverless-api",
  "description": "",
  "version": "0.1.0",
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "postinstall": "pip3 install -r requirements.txt",
    "test": "pytest"
  }
}